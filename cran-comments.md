## R CMD check results

0 errors | 0 warnings | 2 notes

- **New submission**: This is the initial submission of `batchify` to CRAN.
- **Unable to verify current time**: This note is caused by a network
  connectivity issue on the check server and is unrelated to the package.

## Test environments

- Local: R 4.4.3 on Red Hat Enterprise Linux 9.6 (x86_64)
- GitHub Actions:
  - macOS (latest), R release
  - Windows (latest), R release
  - Ubuntu (latest), R devel
  - Ubuntu (latest), R release
  - Ubuntu (latest), R oldrel-1

## Downstream dependencies

None (new package).
